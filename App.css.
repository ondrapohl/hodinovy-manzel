/* Základní styl těla a font */
body {
  margin: 0;
  font-family: 'Helvetica', sans-serif;
  background-color: #111;
  color: #fff;
}

/* Hero sekce */
.hero {
  text-align: center;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  position: relative;
  overflow: hidden;
}

/* Animace plovoucích postav */
.animate-float {
  animation: float 3s ease-in-out infinite;
}

/* Animace postupného zobrazení */
.animate-fadeIn {
  animation: fadeIn 1s ease forwards;
}

/* Animace posouvání nahoru */
.animate-slideUp {
  animation: slideUp 0.8s ease forwards;
}

/* Animace bounce pro tlačítko */
.animate-bounce {
  animation: bounce 2s infinite;
}

/* Service cards */
.service-card {
  background-color: #1f1f1f;
  padding: 20px;
  border-radius: 12px;
  transition: transform 0.3s;
}
.service-card:hover {
  transform: scale(1.05);
}

/* Rezervační formulář */
form input, form select, form textarea {
  width: 100%;
  padding: 12px;
  margin-bottom: 12px;
  border-radius: 8px;
  border: 1px solid #333;
  background-color: #222;
  color: #fff;
}

form button {
  background-color: #1e90ff;
  color: #fff;
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  transition: background-color 0.3s;
}
form button:hover {
  background-color: #0066cc;
}

/* Animace keyframes */
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
}

@keyframes fadeIn {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes slideUp {
  0% { opacity: 0; transform: translateY(40px); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes bounce {
  0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
  40% { transform: translateY(-15px); }
  60% { transform: translateY(-7px); }
}

/* Kontakty */
.contact p {
  margin: 4px 0;
}

/* Responsivní grid pro služby */
.services .grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
  max-width: 1200px;
  margin: 0 auto;
}
@media(min-width: 768px) {
  .services .grid {
    grid-template-columns: repeat(3, 1fr);
  }
}
