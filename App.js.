import React from 'react';
import './App.css';

function Hero() {
  return (
    <section className="hero bg-gradient-to-b from-black via-gray-900 to-gray-800 text-white h-screen flex flex-col justify-center items-center relative overflow-hidden">
      <div className="animated-characters mb-8 animate-float">
        {/* Sem vlož URL obrázku postav */}
        <img src="https://i.imgur.com/TVh1xUf.png" alt="Namakaní chlapi" className="w-64" />
      </div>
      <h1 className="text-5xl font-bold mb-4 animate-fadeIn">
        Hodinový Manžel – Kdykoliv potřebuješ!
      </h1>
      <p className="text-xl mb-6 text-gray-300 text-center max-w-xl animate-fadeIn delay-200">
        Profesionální služby na hodinu. Luxusní, rychlé a diskrétní.
      </p>
      <a href="#rezervace" className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded transition-all duration-300 animate-bounce">
        Rezervovat hned
      </a>
    </section>
  );
}

function Services() {
  return (
    <section id="sluzby" className="services py-20 bg-gray-900 text-white">
      <h2 className="text-4xl font-bold text-center mb-12 animate-fadeIn">Naše služby</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-12 max-w-6xl mx-auto">
        <div className="service-card p-6 bg-gray-800 rounded-lg hover:scale-105 transform transition duration-300 animate-slideUp">
          <h3 className="text-2xl font-semibold mb-2">Montáže a opravy</h3>
          <p>Rychle a spolehlivě opravíme, namontujeme nebo zkontrolujeme cokoli.</p>
        </div>
        <div className="service-card p-6 bg-gray-800 rounded-lg hover:scale-105 transform transition duration-300 animate-slideUp delay-100">
          <h3 className="text-2xl font-semibold mb-2">Elektronika a instalace</h3>
          <p>Od televize po osvětlení – vše profesionálně a diskrétně.</p>
        </div>
        <div className="service-card p-6 bg-gray-800 rounded-lg hover:scale-105 transform transition duration-300 animate-slideUp delay-200">
          <h3 className="text-2xl font-semibold mb-2">Úklid a drobné služby</h3>
          <p>Pomoc s úklidem, přenosem nábytku a všemi drobnými pracemi.</p>
        </div>
      </div>
    </section>
  );
}

function Reservation() {
  return (
    <section id="rezervace" className="reservation py-20 bg-gray-800 text-white">
      <h2 className="text-4xl font-bold text-center mb-12 animate-fadeIn">Rezervace online</h2>
      <form className="max-w-2xl mx-auto bg-gray-900 p-8 rounded-lg shadow-lg animate-slideUp">
        <div className="mb-4">
          <label className="block mb-2" htmlFor="name">Jméno</label>
          <input className="w-full p-3 rounded bg-gray-700 border border-gray-600" type="text" id="name" placeholder="Tvoje jméno" />
        </div>
        <div className="mb-4">
          <label className="block mb-2" htmlFor="email">Email</label>
          <input className="w-full p-3 rounded bg-gray-700 border border-gray-600" type="email" id="email" placeholder="Tvoje emailová adresa" />
        </div>
        <div className="mb-4">
          <label className="block mb-2" htmlFor="service">Služba</label>
          <select className="w-full p-3 rounded bg-gray-700 border border-gray-600" id="service">
            <option>Montáže a opravy</option>
            <option>Elektronika a instalace</option>
            <option>Úklid a drobné služby</option>
          </select>
        </div>
        <div className="mb-4">
          <label className="block mb-2" htmlFor="date">Datum</label>
          <input className="w-full p-3 rounded bg-gray-700 border border-gray-600" type="date" id="date" />
        </div>
        <button className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded transition-all duration-300" type="submit">
          Odeslat rezervaci
        </button>
      </form>
    </section>
  );
}

function Contact() {
  return (
    <section id="kontakty" className="contact py-20 bg-gray-900 text-white">
      <h2 className="text-4xl font-bold text-center mb-12 animate-fadeIn">Kontakt</h2>
      <div className="max-w-4xl mx-auto text-center animate-slideUp">
        <p className="mb-2">Telefon: +420 123 456 789</p>
        <p className="mb-2">Email: info@hodinovymanzel.cz</p>
        <p>Sledujte nás na sociálních sítích: Instagram, Facebook</p>
      </div>
    </section>
  );
}

function App() {
  return (
    <div className="App font-sans">
      <Hero />
      <Services />
      <Reservation />
      <Contact />
    </div>
  );
}

export default App;
